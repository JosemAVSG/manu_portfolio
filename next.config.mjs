/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["source.unsplash.com", "images.pexels.com"],
        unoptimized: true
    }
};

export default nextConfig;
